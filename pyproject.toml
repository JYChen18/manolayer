[project]
name = "manolayer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "roma>=1.5.0",
    "scipy>=1.10.0",
    "torch>=2.0.0",
]

[tool.setuptools]
py-modules = ["manolayer"]
